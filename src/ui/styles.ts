// Shared Tailwind class strings reused across components.

/** Styles a native file input so its "Choose files" button reads as a real,
 *  clickable button (Tailwind `file:` variants target the button part). */
export const fileInputClass =
  'max-w-full text-sm text-gray-500 file:mr-3 file:cursor-pointer file:rounded ' +
  'file:border-0 file:bg-emerald-50 file:px-3 file:py-1.5 file:text-sm ' +
  'file:font-medium file:text-emerald-700 hover:file:bg-emerald-100'
